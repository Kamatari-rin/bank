apiVersion: v1
kind: Service
metadata:
  name: exchange-generator
  namespace: {{ .Release.Namespace }}
  labels: { app: exchange-generator }
spec:
  selector: { app: exchange-generator }
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
  type: ClusterIP
