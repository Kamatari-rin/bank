apiVersion: v1
kind: Service
metadata:
  name: exchange
  namespace: {{ .Release.Namespace }}
  labels: { app: exchange }
spec:
  selector: { app: exchange }
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
  type: ClusterIP
