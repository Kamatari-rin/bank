apiVersion: v1
kind: Service
metadata:
  name: transfer
  namespace: {{ .Release.Namespace }}
  labels: { app: transfer }
spec:
  selector: { app: transfer }
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
  type: ClusterIP
