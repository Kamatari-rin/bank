apiVersion: v1
kind: Service
metadata:
  name: blocker
  namespace: {{ .Release.Namespace }}
  labels: { app: blocker }
spec:
  selector: { app: blocker }
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
  type: ClusterIP
