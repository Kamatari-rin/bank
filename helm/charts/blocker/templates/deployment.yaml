apiVersion: apps/v1
kind: Deployment
metadata:
  name: blocker
  namespace: {{ .Release.Namespace }}
  labels: { app: blocker }
spec:
  replicas: 1
  selector: { matchLabels: { app: blocker } }
  template:
    metadata:
      labels: { app: blocker }
    spec:
      containers:
        - name: blocker
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
          env:
            - name: LOG_KAFKA_BOOTSTRAP_SERVERS
              value: "{{ .Values.env.LOG_KAFKA_BOOTSTRAP_SERVERS }}"
            - name: LOG_KAFKA_TOPIC
              value: "{{ .Values.env.LOG_KAFKA_TOPIC }}
            - name: ZIPKIN_ENDPOINT
              value: "{{ .Values.env.ZIPKIN_ENDPOINT }}"
            - name: SPRING_CONFIG_ADDITIONAL_LOCATION
              value: "file:/config/"
            - name: SPRING_APPLICATION_NAME
              value: "{{ .Values.env.SPRING_APPLICATION_NAME }}"
            - name: SPRING_PROFILES_ACTIVE
              value: "{{ .Values.env.SPRING_PROFILES_ACTIVE }}"
            - name: JAVA_OPTS
              value: "{{ .Values.env.JAVA_OPTS }}"
            - name: OAUTH2_JWK_SET_URI
              value: "{{ .Values.env.OAUTH2_JWK_SET_URI }}"
            - name: NOTIFICATIONS_BASE_URL
              value: "{{ .Values.env.NOTIFICATIONS_BASE_URL }}"
          volumeMounts:
            - name: app-config
              mountPath: /config
              readOnly: true
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8080 }
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            httpGet: { path: /actuator/health, port: 8080 }
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 3
          resources: {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: app-config
          configMap:
            name: blocker-app-config
            items:
              - key: application.yml
                path: application.yml
