apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-keycloak-smoke"
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: curl
      image: curlimages/curl:8.9.1
      args:
        - -fsSL
        - --retry
        - "20"
        - --retry-connrefused
        - "http://keycloak-keycloak.{{ .Release.Namespace }}.svc:8080/realms/master"
